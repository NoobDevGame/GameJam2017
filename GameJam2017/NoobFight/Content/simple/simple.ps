#version 130
uniform sampler2DArray TileTextures;

in vec2 psTexcoord;
flat in uint psTexIndex;

out vec4 outColor;
void main()
{
	vec4 texColor = texture(TileTextures,vec3(psTexcoord,psTexIndex-1u));
	outColor = texColor;
}
